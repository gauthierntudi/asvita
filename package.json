{
  "name": "asvita",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -k -n server,client -c red,blue \"npm run dev -w server\" \"npx --yes wait-on@8.0.1 http-get://127.0.0.1:4000/api/health -t 60000 && npm run dev -w client\"",
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run dev -w server",
    "build": "npm run build -w client && npm run build -w server",
    "vercel-build": "npm run build -w server && node scripts/prepare-vercel-api.mjs && npm install --omit=dev --prefix api && npm run build -w client",
    "postinstall": "npm run db:generate -w server",
    "db:generate": "npm run db:generate -w server",
    "db:push": "npm run db:push -w server",
    "db:migrate": "npm run db:migrate -w server",
    "db:studio": "npm run db:studio -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
